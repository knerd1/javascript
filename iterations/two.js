     let i = 0
     while (i<=10) {
         // console.log(`Value of i is ${i}`);
          i = i +2
     }


     const myArray = ['gon', 'luffy', 'nami']
     let arr = 0
     while (arr < myArray.length) {
          console.log(`value is ${myArray[arr]}`);
          arr = arr +1
     }

     let score =0
     do {
          console.log(`score is ${score}`);
          score++
     } while (score<=10);